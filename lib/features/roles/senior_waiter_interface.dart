import 'package:flutter/material.dart';

class SeniorWaiterInterface extends StatelessWidget {
  const SeniorWaiterInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Senior Waiter Dashboard')), body: const Center(child: Text('Senior Waiter Interface')));
  }
}
