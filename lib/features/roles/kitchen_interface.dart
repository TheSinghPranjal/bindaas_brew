import 'package:flutter/material.dart';

class KitchenInterface extends StatelessWidget {
  const KitchenInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Kitchen Dashboard')), body: const Center(child: Text('Kitchen Interface')));
  }
}
