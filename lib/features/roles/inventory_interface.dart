import 'package:flutter/material.dart';

class InventoryInterface extends StatelessWidget {
  const InventoryInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Inventory Dashboard')), body: const Center(child: Text('Inventory Interface')));
  }
}
