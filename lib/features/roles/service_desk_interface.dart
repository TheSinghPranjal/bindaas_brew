import 'package:flutter/material.dart';

class ServiceDeskInterface extends StatelessWidget {
  const ServiceDeskInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Service Desk Dashboard')), body: const Center(child: Text('Service Desk Interface')));
  }
}
