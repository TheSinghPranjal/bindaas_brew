import 'package:flutter/material.dart';

class CleaningInterface extends StatelessWidget {
  const CleaningInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Cleaning Dashboard')), body: const Center(child: Text('Cleaning Interface')));
  }
}
