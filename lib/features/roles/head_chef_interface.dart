import 'package:flutter/material.dart';

class HeadChefInterface extends StatelessWidget {
  const HeadChefInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Head Chef Dashboard')), body: const Center(child: Text('Head Chef Interface')));
  }
}
