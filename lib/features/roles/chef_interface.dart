import 'package:flutter/material.dart';

class ChefInterface extends StatelessWidget {
  const ChefInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Chef Dashboard')), body: const Center(child: Text('Chef Interface')));
  }
}
